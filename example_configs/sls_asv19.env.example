# ================================
# Required Paths — Must be set
# ================================
# SSL_DATABASE_PATH: Absolute path to the ASV19 (or other) dataset root
# SSL_PROTOCOLS_PATH: Absolute path to the folder containing protocol files
# SSL_TRAIN_PROTOCOL: Training protocol filename (e.g., ASVspoof2019_train_protocol.txt)
# SSL_DEV_PROTOCOL: Development protocol filename (e.g., ASVspoof2019_dev_protocol.txt)

SSL_DATABASE_PATH=/absolute/path/to/ASV19
SSL_PROTOCOLS_PATH=/absolute/path/to/ASV19/protocols
SSL_TRAIN_PROTOCOL=ASVspoof2019_train_protocol.txt
SSL_DEV_PROTOCOL=ASVspoof2019_dev_protocol.txt

# ================================
# Protocol Format Settings — Update if your protocol file uses a different format
# ================================
# SSL_PROTOCOL_DELIMITER: Delimiter used in protocol files (" ", "," or "\t")
# SSL_PROTOCOL_KEY_COL: Index of the column containing the utterance ID
# SSL_PROTOCOL_LABEL_COL: Index of the column containing the label (e.g., 4 for ASVspoof2019)

SSL_PROTOCOL_DELIMITER=" "
SSL_PROTOCOL_KEY_COL=0
SSL_PROTOCOL_SRC_COL=3
SSL_PROTOCOL_LABEL_COL=4

# ================================
# Model & Run Settings
# ================================
SSL_MODEL_ARCH=sls
SSL_MODE=train
SSL_MODEL_NAME=aasist_ASV19
CUDA_DEVICE=cuda:0